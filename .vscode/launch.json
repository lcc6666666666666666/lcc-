{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "cppvsdbg",
            "request": "launch",
            "name": "Debug (MSVC)",
            "program": "${workspaceFolder}\\compiler.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build with MSVC",
            "console": "integratedTerminal"
        }
    ]
}
